# 📱 MPT Schedule App - Полная техническая документация

> **Версия:** 2.0  
> **Дата:** Декабрь 2024  
> **Разработчик:** Джаваншир Махмудов

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Структура проекта](#структура-проекта)
4. [Основные компоненты](#основные-компоненты)
5. [Экраны приложения](#экраны-приложения)
6. [API и сервер](#api-и-сервер)
7. [Хранение данных](#хранение-данных)
8. [Обновление контента](#обновление-контента)

---

## 🎯 Обзор проекта

**MPT Schedule App** — мобильное приложение для студентов Московского приборостроительного техникума, предоставляющее:

- 📅 **Расписание занятий** с поддержкой числителя/знаменателя
- 📝 **Домашние задания** с возможностью добавления, редактирования и отметки выполнения
- 🔔 **Расписание звонков**
- 📰 **Новости колледжа** с фотогалереей
- ⭐ **Рейтинг преподавателей** с системой голосования
- 💡 **Рекомендации** полезных ресурсов (курсы, Telegram-каналы, YouTube)
- ⚙️ **Настройки** (изменение группы, размер текста, цвета недель)

### Технологии

- **iOS:** SwiftUI, Swift 5.0+
- **Backend:** Python 3.13, FastAPI, BeautifulSoup
- **Хостинг:** Render.com
- **Хранение:** UserDefaults (локально), in-memory cache (сервер)

---

## 🏗️ Архитектура

### Клиент-серверная архитектура

```
┌─────────────────┐
│   iOS App       │  ← SwiftUI приложение
│   (Swift)       │
└────────┬────────┘
         │ HTTPS
         ▼
┌─────────────────┐
│   FastAPI       │  ← Python сервер
│   (Render.com)  │
└────────┬────────┘
         │ HTTP
         ▼
┌─────────────────┐
│   mpt.ru        │  ← Парсинг расписания
│   (BeautifulSoup)│
└─────────────────┘
```

### Поток данных

1. **Парсинг:** Сервер парсит `mpt.ru/raspisanie/` и `mpt.ru/izmeneniya-v-raspisanii/`
2. **Кеширование:** Данные кешируются на сервере (5 минут) и в приложении (24 часа)
3. **Синхронизация:** Приложение проверяет обновления каждые 5 минут

---

## 📁 Структура проекта

```
mptapp/
├── MPTapp/                    # iOS приложение
│   ├── MPTapp/
│   │   ├── MPTappApp.swift    # Точка входа
│   │   ├── RootView.swift     # Корневой view
│   │   ├── OnboardingViews.swift  # Экран приветствия
│   │   ├── MainScreens.swift  # Основные экраны (Сегодня, Неделя, Звонки, Настройки)
│   │   ├── NewsView.swift     # Экран новостей и рекомендаций
│   │   ├── Models.swift       # Модели данных
│   │   ├── NetworkService.swift  # Сетевые запросы
│   │   ├── StorageService.swift   # Локальное хранение
│   │   ├── ScheduleViewModel.swift  # Логика расписания
│   │   ├── ContentUpdateService.swift  # Обновление контента
│   │   ├── TeacherRatingService.swift  # Рейтинг преподавателей
│   │   ├── AppSettingsService.swift    # Настройки приложения
│   │   ├── Assets.xcassets/   # Изображения и иконки
│   │   └── news/              # Фото для новостей
│   └── MPTapp.xcodeproj/      # Xcode проект
│
└── server/                    # Backend сервер
    ├── main.py               # FastAPI приложение
    ├── parser.py             # Парсинг HTML
    ├── models.py             # Модели данных (Pydantic)
    ├── requirements.txt      # Python зависимости
    └── venv/                 # Виртуальное окружение
```

---

## 🔧 Основные компоненты

### iOS приложение

#### 1. **Models.swift**
Определяет все структуры данных:
- `Specialty`, `Group` — специальности и группы
- `Lesson`, `DaySchedule`, `WeekSchedule` — расписание
- `Homework` — домашние задания
- `Replacement` — замены в расписании
- `TeacherRating`, `VoteType` — рейтинг преподавателей
- `Advertisement`, `NewsItem` — рекомендации и новости

#### 2. **NetworkService.swift**
- `actor NetworkService` — потокобезопасный сервис для API запросов
- Методы: `fetchSpecialties()`, `fetchGroups()`, `fetchSchedule()`, `fetchReplacements()`, `fetchTeachers()`

#### 3. **StorageService.swift**
- Локальное хранение через `UserDefaults`
- Кеширование: расписание, ДЗ, настройки, контент
- TTL: 24 часа для расписания

#### 4. **ScheduleViewModel.swift**
- `ObservableObject` для управления состоянием расписания
- Загрузка данных с сервера
- Обработка числителя/знаменателя

#### 5. **ContentUpdateService.swift**
- Автоматическое обновление рекомендаций и новостей
- Проверка версии контента каждые 5 минут
- Кеширование контента локально

#### 6. **TeacherRatingService.swift**
- Управление рейтингом преподавателей
- Система голосования (лайк/нейтрал/дизлайк)
- Расписание голосования: Пн 00:00 - Сб 17:00

#### 7. **AppSettingsService.swift**
- Настройки пользователя: размер текста, цвета недель
- Сохранение в `UserDefaults`

### Backend сервер

#### 1. **main.py**
FastAPI приложение с endpoints:
- `GET /api/week-info` — информация о неделе (числитель/знаменатель)
- `GET /api/specialties` — список специальностей
- `GET /api/groups?specialty_id=...` — группы по специальности
- `GET /api/schedule?group=...&specialty_id=...` — расписание группы
- `GET /api/replacements?group=...` — замены в расписании
- `GET /api/teachers` — список всех преподавателей
- `GET /api/content/advertisements` — рекомендации
- `GET /api/content/news` — новости
- `GET /api/content/version` — версия контента

#### 2. **parser.py**
Парсинг HTML с помощью BeautifulSoup:
- `parse_week_info()` — определение числителя/знаменателя
- `parse_specialties()` — извлечение специальностей
- `parse_groups_for_specialty()` — группы
- `parse_schedule_for_group()` — расписание
- `parse_replacements()` — замены

#### 3. **models.py**
Pydantic модели для валидации данных

---

## 📱 Экраны приложения

### 1. **Экран приветствия (Onboarding)**

**Файл:** `OnboardingViews.swift`

**Этапы:**
1. **Welcome Screen** — приветствие с логотипом МПТ
2. **Select Specialty** — выбор специальности из списка
3. **Select Group** — выбор группы

**Особенности:**
- Данные загружаются с сервера
- Выбор сохраняется в `@AppStorage`
- При повторном запуске пропускается

---

### 2. **Сегодня (TodayView)**

**Файл:** `MainScreens.swift`

**Функции:**
- Показывает расписание на текущий день
- Динамический градиент в зависимости от недели (числитель/знаменатель)
- Отображение локации (Нахимовский/Неженская)
- Карточки пар с временем, предметом, преподавателем
- Поддержка сдвоенных пар (числитель/знаменатель)
- Добавление домашних заданий к каждой паре
- Отображение замен в расписании
- Рекламные баннеры внизу

**Компоненты:**
- `LessonCard` — карточка пары
- `WeekTypeBadge` — бейдж числителя/знаменателя
- `CampusBadge` — бейдж локации
- `HomeworkEditorView` — редактор ДЗ
- `ReplacementRow` — строка замены

---

### 3. **Неделя (WeekView)**

**Файл:** `MainScreens.swift`

**Функции:**
- Календарь с выбором недели/дня
- Полное расписание недели
- Переключение между видом недели и отдельного дня
- Добавление ДЗ к парам в любом дне
- Индикаторы дней с ДЗ
- Сворачивание/разворачивание календаря

**Компоненты:**
- `WeekCalendarView` — календарь
- `WeekDaySection` — секция дня недели
- `SundayCard` — карточка выходного дня

---

### 4. **Звонки (BellsView)**

**Файл:** `MainScreens.swift`

**Функции:**
- Расписание звонков
- Время начала и окончания пар
- Время перемен

---

### 5. **Новости (NewsView)**

**Файл:** `NewsView.swift`

**Функции:**
- **Карусель новостей** — автоматическая прокрутка фотографий
- **Рекомендации** — раскрывающиеся карточки с фильтрацией по категориям
- **Рейтинг преподавателей** — топ-3, топ-10, все преподаватели

**Компоненты:**
- `NewsCard` — карточка новости
- `RecommendationCard` — карточка рекомендации
- `CategoryFilterButton` — кнопка фильтра категорий
- `TeacherRow` — строка преподавателя
- `Top3Card` — карточка топ-3

---

### 6. **Настройки (SettingsView)**

**Файл:** `MainScreens.swift`

**Функции:**
- Изменение специальности и группы
- Регулировка размера текста (0.8x - 1.4x)
- Выбор цветов для числителя/знаменателя
- Сброс настроек

---

## 🌐 API и сервер

### Endpoints

#### Расписание
- `GET /api/week-info` → `WeekInfo`
- `GET /api/specialties` → `List[Specialty]`
- `GET /api/groups?specialty_id={id}` → `List[Group]`
- `GET /api/schedule?group={name}&specialty_id={id}` → `ScheduleResponse`
- `GET /api/replacements?group={name}` → `ReplacementsResponse`
- `GET /api/teachers` → `List[str]`

#### Контент
- `GET /api/content/advertisements` → `List[Advertisement]`
- `GET /api/content/news` → `List[NewsItem]`
- `GET /api/content/version` → `{"version": "2.0"}`

### Кеширование на сервере

- **HTML страницы:** 5 минут
- **Parsed данные:** 5 минут
- **In-memory cache** в `cache` словаре

### Деплой на Render.com

1. Push в GitHub
2. Render автоматически деплоит (или Manual Deploy)
3. URL: `https://your-app.onrender.com`

---

## 💾 Хранение данных

### Локальное (UserDefaults)

**Ключи:**
- `selectedSpecialtyId`, `selectedGroupId` — выбор пользователя
- `cached_schedule_{group}` — расписание
- `cached_week_info` — информация о неделе
- `cached_homeworks` — домашние задания
- `cached_advertisements`, `cached_news_items` — контент
- `teacher_ratings` — рейтинги преподавателей
- `user_votes` — голоса пользователя
- `appTextScale`, `numeratorColorIndex`, `denominatorColorIndex` — настройки

**TTL:**
- Расписание: 24 часа
- Контент: до обновления версии

---

## 🔄 Обновление контента

### Автоматическое обновление

1. **При запуске приложения** — проверка версии контента
2. **При открытии вкладки "Новости"** — проверка обновлений
3. **Каждые 5 минут** — автоматическая проверка в фоне

### Как обновить контент

1. Открой `server/main.py`
2. Измени массив `advertisements` или `news`
3. Увеличь `CONTENT_VERSION` (например, "2.0" → "2.1")
4. `git add server/main.py`
5. `git commit -m "Обновил контент"`
6. `git push`
7. На Render.com: Manual Deploy
8. Пользователи получат обновление через 1-5 минут

### Версионирование

Формат: `"MAJOR.MINOR"` (например, "2.0", "2.1", "3.0")

- **MAJOR** — большие изменения (новая структура данных)
- **MINOR** — мелкие изменения (новые рекомендации, тексты)

---

## 🔐 Безопасность

### Текущие меры
- CORS настроен для всех источников (для разработки)
- Нет авторизации (публичный API)
- Нет валидации входных данных (только на уровне парсера)

### Рекомендации для продакшена
- Добавить rate limiting
- Добавить авторизацию для админ-панели
- Валидация всех входных данных
- HTTPS только

---

## 📊 Производительность

### Оптимизации
- Кеширование на сервере (5 минут)
- Кеширование в приложении (24 часа)
- Lazy loading для списков
- Асинхронная загрузка данных

### Ограничения
- Render.com free tier: 750 часов/месяц
- In-memory cache (теряется при перезапуске)
- Нет CDN для статики

---

## 🐛 Известные проблемы

1. **Парсинг расписания** — может сломаться при изменении HTML структуры сайта
2. **Кеш на Render.com** — сбрасывается при перезапуске сервера
3. **Фото в Assets** — требуют пересборки приложения

---

## 📈 Планы развития

### Краткосрочные
- [ ] Загрузка фото рекомендаций с сервера
- [ ] Админ-панель для управления контентом
- [ ] Push-уведомления о заменах

### Долгосрочные
- [ ] Публикация в App Store
- [ ] Android версия
- [ ] Интеграция с календарём iOS
- [ ] Экспорт расписания в PDF

---

## 📞 Контакты и поддержка

**Разработчик:** Джаваншир Махмудов  
**GitHub:** [Klokkloki/MPT_app](https://github.com/Klokkloki/MPT_app)  
**Сервер:** Render.com

---

*Последнее обновление: Декабрь 2024*

