# 📸 Как добавить фотографии в новости

## ✅ Правильный способ

### 1. Добавь фото в проект Xcode

1. Открой проект в Xcode
2. Найди папку `MPTapp/MPTapp/news/` в Project Navigator
3. Перетащи фото в эту папку (или правый клик → Add Files to "MPTapp")
4. **ВАЖНО:** Убедись, что в File Inspector стоит галочка "MPTapp" в Target Membership

### 2. Обнови `server/main.py`

Открой файл `server/main.py` и найди функцию `get_content_news()`:

```python
@app.get("/api/content/news") 
async def get_content_news():
    news = [
        {
            "id": "1",
            "imageName": "00.10.2024",  # ← Имя файла БЕЗ расширения!
            "title": "Экскурсия",
            "description": "Студенты МПТ на экскурсии"
        },
        {
            "id": "2", 
            "imageName": "news_1",  # ← Если файл называется news_1.webp
            "title": "Новости колледжа",
            "description": "Следите за событиями"
        }
    ]
    return {"news": news}
```

**Правила:**
- ✅ Указывай только имя файла **БЕЗ расширения** (`.webp`, `.jpg`, `.png`)
- ✅ Если файл называется `photo.jpg` → пиши `"imageName": "photo"`
- ✅ Если файл называется `news_1.webp` → пиши `"imageName": "news_1"`

### 3. Увеличь версию контента

Найди в `server/main.py` строку:

```python
CONTENT_VERSION = "1.2"  # ← Увеличь на 0.1 (например, "1.3")
```

### 4. Пересобери приложение

В Xcode:
- `Product` → `Clean Build Folder` (⇧⌘K)
- `Product` → `Build` (⌘B)

### 5. Задеплой обновления

```bash
cd ~/Documents/mptapp
git add .
git commit -m "Добавил новую фотографию в новости"
git push
```

Затем на Render.com:
- Открой свой сервис
- Нажми "Manual Deploy" → "Deploy latest commit"

### 6. Готово! 🎉

Пользователи получат новую фотографию через 1-5 минут после деплоя.

---

## ❌ ЧТО НЕ НУЖНО ДЕЛАТЬ

### ❌ НЕ указывай полный путь к файлу:

```python
# ❌ НЕПРАВИЛЬНО!
"imageName": "/Users/dzavansirmahmudov/Documents/mptapp/MPTapp/MPTapp/news/news_1.webp"
```

**Почему:** Сервер на Render.com не имеет доступа к файлам на твоем Mac!

### ❌ НЕ указывай расширение файла:

```python
# ❌ НЕПРАВИЛЬНО!
"imageName": "news_1.webp"
```

**Почему:** Приложение само найдет файл с нужным расширением (`.webp`, `.jpg`, `.png`)

---

## 📋 Поддерживаемые форматы

- ✅ `.jpg` / `.jpeg` (рекомендуется)
- ✅ `.png`
- ✅ `.webp` (iOS 14+)

**Все форматы работают одинаково!** Просто укажи имя файла без расширения.

---

## 🔍 Проверка

После добавления фото:

1. Убедись, что файл есть в папке `news/` в Xcode
2. Убедись, что файл добавлен в Target "MPTapp"
3. Пересобери приложение
4. Запусти приложение и открой вкладку "Новости"
5. Если фото не показывается, проверь имя в `imageName` (должно совпадать с именем файла без расширения)

---

## 💡 Примеры

| Файл в проекте | `imageName` в `main.py` |
|----------------|-------------------------|
| `photo.jpg` | `"photo"` |
| `news_1.webp` | `"news_1"` |
| `event.png` | `"event"` |
| `00.10.2024.jpg` | `"00.10.2024"` |

---

**Вопросы?** Проверь файл `CONTENT_MANAGEMENT.md` для подробной документации.

