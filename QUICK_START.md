# ⚡ Быстрый старт для iPhone

## 🎯 За 3 шага подключить iPhone к серверу

### 1️⃣ Найти IP адрес Mac

Выполните в Терминале:
```bash
cd ~/Documents/mptapp
./get_mac_ip.sh
```

Или вручную:
- **Системные настройки** → **Сеть** → **Wi-Fi** → скопируйте IP адрес

### 2️⃣ Обновить NetworkService.swift

Откройте: `MPTapp/MPTapp/NetworkService.swift`

Найдите строку 152:
```swift
private let baseURL = "http://192.168.1.XXX:8000"
```

Замените `XXX` на ваш IP, например:
```swift
private let baseURL = "http://192.168.1.105:8000"
```

### 3️⃣ Запустить сервер и пересобрать приложение

**Терминал:**
```bash
cd ~/Documents/mptapp/server
source venv/bin/activate
python main.py
```

**Xcode:**
- Нажмите **Cmd+B** (Build)
- Выберите iPhone в списке устройств
- Нажмите **Cmd+R** (Run)

---

## 🎨 Добавить иконку (опционально)

1. Создайте изображение **1024x1024px** (PNG)
2. В Xcode: `Assets.xcassets` → `AppIcon`
3. Перетащите изображение в область иконки
4. Пересоберите приложение

**Быстрый вариант:** Используйте онлайн генератор:
- https://www.appicon.co
- Загрузите одно изображение, получите все размеры

---

## ✅ Готово!

Теперь приложение на iPhone будет получать актуальные данные с сервера.

**Важно:** Mac и iPhone должны быть в **одной Wi-Fi сети**!

