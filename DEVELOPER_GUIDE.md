# 👨‍💻 Руководство разработчика MPT Schedule App

> **Для:** Разработчиков и администраторов проекта  
> **Версия:** 2.0

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Настройка окружения](#настройка-окружения)
3. [Обновление контента](#обновление-контента)
4. [Добавление новых функций](#добавление-новых-функций)
5. [Деплой](#деплой)
6. [Отладка](#отладка)
7. [Частые задачи](#частые-задачи)

---

## 🚀 Быстрый старт

### Клонирование проекта

```bash
git clone https://github.com/Klokkloki/MPT_app.git
cd mptapp
```

### Запуск сервера локально

```bash
cd server
source venv/bin/activate
python main.py
```

Сервер запустится на `http://localhost:8000`

### Запуск iOS приложения

1. Открой `MPTapp/MPTapp.xcodeproj` в Xcode
2. Выбери симулятор или подключённый iPhone
3. Нажми `⌘R` (Run)

---

## ⚙️ Настройка окружения

### iOS приложение

**Требования:**
- Xcode 15.0+
- iOS 17.0+
- Swift 5.0+

**Настройка URL сервера:**

Открой `MPTapp/MPTapp/NetworkService.swift`:

```swift
private let _baseURL = "https://your-app.onrender.com"  // Замени на свой URL
```

### Backend сервер

**Требования:**
- Python 3.13+
- pip

**Установка зависимостей:**

```bash
cd server
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

**Зависимости:**
- `fastapi` — веб-фреймворк
- `uvicorn` — ASGI сервер
- `beautifulsoup4` — парсинг HTML
- `httpx` — HTTP клиент

---

## 📝 Обновление контента

### 1. Обновление рекомендаций

**Файл:** `server/main.py`

Найди функцию `get_content_advertisements()` и измени массив:

```python
advertisements = [
    {
        "id": "unique-id",
        "title": "Название",
        "subtitle": "Короткое описание",
        "description": "Полное описание...",
        "iconName": "logo_name",  # Или "iconEmoji": "🎯"
        "url": "https://example.com",
        "category": "telegram",  # или "youtube", "course", etc.
        "tags": ["бесплатно", "новое"],
        "gradientColors": ["#FF6B6B", "#4ECDC4"],
        "isPinned": False
    }
]
```

**Категории:**
- `telegram` — Telegram каналы
- `youtube` — YouTube каналы
- `course` — Курсы
- `onlineSchool` — Онлайн-школы
- `service` — Сервисы
- `event` — События

**Теги (автоматические цвета):**
- `бесплатно` / `free` → 🟢 зелёный
- `скидка` / `sale` → 🟠 оранжевый
- `новое` / `new` → 🔵 синий
- `топ` / `hot` → 🔴 красный

**Обязательно:**
1. Увеличь `CONTENT_VERSION` (например, "2.0" → "2.1")
2. `git add server/main.py`
3. `git commit -m "Обновил рекомендации"`
4. `git push`
5. На Render.com: Manual Deploy

---

### 2. Обновление новостей

**Файл:** `server/main.py`

Найди функцию `get_content_news()`:

```python
news = [
    {
        "id": "1",
        "imageName": "news_0",  # Имя файла БЕЗ расширения
        "title": "Заголовок",
        "description": "Описание"
    }
]
```

**Добавление фото:**
1. Добавь фото в `MPTapp/MPTapp/Assets.xcassets/` как Image Set
2. Назови его (например, `news_0`)
3. В `main.py` укажи `"imageName": "news_0"`
4. Пересобери приложение в Xcode

**Форматы:** `.png`, `.jpg` (`.webp` не поддерживается в Assets)

**Рекомендуемые размеры:**
- Ширина: 800-1200px
- Соотношение: 16:9 или 4:3

---

### 3. Добавление фото-иконки для рекомендации

**Шаг 1:** Подготовь изображение
- Размер: **96×96 px** (минимум) или **144×144 px** (рекомендуется)
- Формат: `.png` (лучше для логотипов)
- Прозрачный фон (опционально)

**Шаг 2:** Добавь в Assets.xcassets
1. Открой Xcode → `Assets.xcassets`
2. Правый клик → **New Image Set**
3. Назови (например, `studsovet_icon`)
4. Перетащи фото в слот **2x**

**Шаг 3:** Обнови `server/main.py`

```python
{
    "id": "tg-1",
    "iconName": "studsovet_icon",  # Вместо iconEmoji
    ...
}
```

**Шаг 4:** Пересобери приложение

---

## 🛠️ Добавление новых функций

### Добавление нового экрана

1. Создай новый файл `NewScreen.swift`:

```swift
import SwiftUI

struct NewScreen: View {
    var body: some View {
        Text("Новый экран")
    }
}
```

2. Добавь в `MainTabView` в `RootView.swift`:

```swift
NewScreen()
    .tabItem {
        Image(systemName: "star.fill")
        Text("Новый")
    }
```

### Добавление нового API endpoint

**Файл:** `server/main.py`

```python
@app.get("/api/new-endpoint")
async def new_endpoint():
    return {"message": "Hello"}
```

**В iOS:** Добавь метод в `NetworkService.swift`:

```swift
func fetchNewData() async throws -> NewData {
    let url = URL(string: "\(baseURL)/api/new-endpoint")!
    let (data, _) = try await URLSession.shared.data(from: url)
    return try JSONDecoder().decode(NewData.self, from: data)
}
```

### Добавление новой модели данных

**Файл:** `MPTapp/MPTapp/Models.swift`

```swift
struct NewModel: Identifiable, Codable {
    let id: UUID
    let name: String
    
    init(id: UUID = UUID(), name: String) {
        self.id = id
        self.name = name
    }
}
```

**Важно:** Все модели должны быть `Codable` для сериализации!

---

## 🚢 Деплой

### Деплой сервера на Render.com

1. **Push в GitHub:**
```bash
git add .
git commit -m "Обновление"
git push
```

2. **На Render.com:**
   - Открой Dashboard → свой сервис
   - Нажми **"Manual Deploy"** → **"Deploy latest commit"**
   - Подожди 2-3 минуты

3. **Проверка:**
```bash
curl https://your-app.onrender.com/api/week-info
```

### Деплой iOS приложения

#### На физическое устройство (через кабель)

1. Подключи iPhone
2. В Xcode выбери устройство
3. Нажми `⌘R` (Run)
4. На iPhone: Настройки → Основные → VPN и управление устройством → Доверься разработчику

#### TestFlight (для тестирования)

1. **Требуется:** Apple Developer Program ($99/год)
2. В Xcode: `Product` → `Archive`
3. `Distribute App` → `App Store Connect`
4. Загрузи в TestFlight
5. Пригласи тестеров

#### App Store (публикация)

1. Заполни информацию в App Store Connect
2. Подготовь скриншоты (6.7" и 6.1" iPhone)
3. Отправь на ревью
4. Ожидай 1-3 дня

---

## 🐛 Отладка

### Проблемы с сервером

**Сервер не отвечает:**
```bash
# Проверь логи на Render.com
# Проверь что сервер запущен локально
curl http://localhost:8000/
```

**Ошибки парсинга:**
- Проверь что сайт `mpt.ru` не изменил структуру HTML
- Обнови селекторы в `parser.py`

### Проблемы с iOS приложением

**Не загружаются данные:**
1. Проверь `NetworkService.baseURL`
2. Проверь интернет-соединение
3. Проверь логи в Xcode Console

**Фото не отображаются:**
1. Проверь что файл добавлен в Assets.xcassets
2. Проверь что файл добавлен в Target "MPTapp"
3. Проверь имя в `imageName` (без расширения!)

**Кеш не обновляется:**
1. Очисти кеш: удали приложение и переустанови
2. Или увеличь `CONTENT_VERSION` на сервере

---

## 📋 Частые задачи

### Изменить цвет темы

**Файл:** `MPTapp/MPTapp/MainScreens.swift` или `NewsView.swift`

Найди `.preferredColorScheme(.dark)` и измени на `.light` или убери.

### Изменить размер шрифта по умолчанию

**Файл:** `MPTapp/MPTapp/AppSettingsService.swift`

```swift
@Published var textScale: Double = 1.0  // Измени значение
```

### Добавить новую категорию рекомендаций

1. **Файл:** `MPTapp/MPTapp/Models.swift`

```swift
enum AdCategory: String, Codable, CaseIterable {
    // ... существующие
    case newCategory = "newCategory"
    
    var displayName: String {
        case .newCategory: return "Новая категория"
    }
    
    var icon: String {
        case .newCategory: return "star.fill"
    }
    
    var defaultColor: Color {
        case .newCategory: return .purple
    }
}
```

2. Используй в `server/main.py`: `"category": "newCategory"`

### Изменить интервал проверки обновлений

**Файл:** `MPTapp/MPTapp/ContentUpdateService.swift`

```swift
updateTimer = Timer.scheduledTimer(
    timeInterval: 300,  // Измени на нужное (в секундах)
    ...
)
```

### Очистить весь кеш приложения

**Файл:** `MPTapp/MPTapp/StorageService.swift`

Добавь метод:

```swift
func clearAllCache() {
    let defaults = UserDefaults.standard
    let keys = defaults.dictionaryRepresentation().keys
    for key in keys {
        if key.hasPrefix("cached_") {
            defaults.removeObject(forKey: key)
        }
    }
}
```

---

## 🔧 Полезные команды

### Git

```bash
# Проверить статус
git status

# Добавить все изменения
git add .

# Коммит
git commit -m "Описание изменений"

# Push
git push

# Посмотреть историю
git log --oneline
```

### Python сервер

```bash
# Активировать venv
source server/venv/bin/activate

# Запустить сервер
cd server && python main.py

# Проверить зависимости
pip list

# Обновить зависимости
pip install -r requirements.txt --upgrade
```

### Xcode

- `⌘B` — Build
- `⌘R` — Run
- `⇧⌘K` — Clean Build Folder
- `⌘.` — Stop
- `⌘/` — Comment/Uncomment

---

## 📚 Полезные ссылки

- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [BeautifulSoup Documentation](https://www.crummy.com/software/BeautifulSoup/bs4/doc/)
- [Render.com Documentation](https://render.com/docs)

---

## ⚠️ Важные замечания

1. **Всегда увеличивай `CONTENT_VERSION`** при изменении контента
2. **Тестируй локально** перед push в GitHub
3. **Проверяй логи** на Render.com после деплоя
4. **Фото в Assets** требуют пересборки приложения
5. **Кеш** может быть причиной старых данных — очисти при необходимости

---

*Последнее обновление: Декабрь 2024*

