# 📱 MPT App - Полное описание проекта

## 📋 Оглавление

1. [Общая информация](#общая-информация)
2. [Структура проекта](#структура-проекта)
3. [Архитектура приложения](#архитектура-приложения)
4. [Работа сервера](#работа-сервера)
5. [Связь приложения с сервером](#связь-приложения-с-сервером)
6. [Развёртывание на Render.com](#развёртывание-на-rendercom)
7. [Безопасность и оптимизация](#безопасность-и-оптимизация)
8. [Основные функции приложения](#основные-функции-приложения)

---

## 🎯 Общая информация

**MPT App** - это мобильное iOS-приложение для студентов Московского приборостроительного техникума. Приложение показывает расписание занятий, замены, домашние задания, новости колледжа и позволяет голосовать за преподавателей.

### Технологии:
- **iOS приложение**: Swift + SwiftUI
- **Сервер**: Python + FastAPI
- **Парсинг**: BeautifulSoup + httpx
- **Хостинг**: Render.com (бесплатный план)

---

## 📁 Структура проекта

### Основные папки:

```
MPT_app/
├── MPTapp/                    # iOS приложение
│   ├── MPTapp/               # Основной код приложения
│   │   ├── MPTappApp.swift   # Точка входа (главный файл)
│   │   ├── RootView.swift    # Корневой экран (онбординг или главный экран)
│   │   ├── Models.swift      # Все модели данных
│   │   ├── NetworkService.swift      # Сетевой слой (запросы к API)
│   │   ├── StorageService.swift      # Локальное хранилище (UserDefaults)
│   │   ├── ScheduleViewModel.swift   # Логика работы с расписанием
│   │   ├── ContentUpdateService.swift # Обновление контента (реклама, новости)
│   │   ├── TeacherRatingService.swift # Рейтинг преподавателей
│   │   ├── AppSettingsService.swift   # Настройки приложения
│   │   ├── MainScreens.swift # Основные экраны (Сегодня, Неделя, Настройки)
│   │   ├── OnboardingViews.swift # Экран выбора группы
│   │   └── Assets.xcassets/  # Изображения и иконки
│   └── MPTapp.xcodeproj/     # Xcode проект
│
└── server/                    # Python сервер
    ├── main.py               # FastAPI приложение (главный файл)
    ├── parser.py             # Парсинг HTML с сайта mpt.ru
    ├── models.py             # Модели данных для API
    ├── requirements.txt      # Зависимости Python
    └── README.md             # Документация сервера
```

---

## 🏗️ Архитектура приложения

### Принцип работы (общая схема):

```
1. Пользователь открывает приложение
   ↓
2. RootView проверяет: выбрана ли группа?
   - Если НЕТ → показывает OnboardingViews (выбор специальности и группы)
   - Если ДА → показывает MainTabView (основной интерфейс)
   ↓
3. MainTabView состоит из 4 вкладок:
   - Сегодня (расписание на сегодня)
   - Неделя (расписание на всю неделю)
   - Звонки (расписание звонков)
   - Настройки (изменение группы, цвета, размера текста)
   ↓
4. При открытии вкладки "Сегодня":
   - Сначала показываются данные из кеша (мгновенно)
   - Затем в фоне запрашиваются обновления с сервера
   - Если сервер недоступен - работаем офлайн с кешем
```

### Основные компоненты:

#### 1. **MPTappApp.swift** - Точка входа
- Запускает приложение
- Инициализирует ContentUpdateService (обновление рекламы/новостей)
- Запускает автоматическую проверку обновлений каждые 5 минут

#### 2. **RootView.swift** - Маршрутизация
- Проверяет, выбрана ли группа пользователем (хранится в UserDefaults)
- Показывает либо онбординг, либо главный экран

#### 3. **ScheduleViewModel.swift** - Бизнес-логика расписания
- Загружает расписание с сервера
- Кеширует данные локально
- Обрабатывает ошибки сети
- Преобразует данные API в модели приложения

#### 4. **NetworkService.swift** - Сетевой слой
- Делает HTTP-запросы к серверу
- URL сервера: `https://mpt-app.onrender.com`
- Все методы асинхронные (async/await)

#### 5. **StorageService.swift** - Локальное хранилище
- Сохраняет расписание, домашние задания, настройки
- Использует UserDefaults (встроенная база данных iOS)
- Реализует кеширование с проверкой свежести данных

---

## 🖥️ Работа сервера

### Что делает сервер?

Сервер написан на Python с использованием FastAPI. Он парсит HTML-страницу с сайта mpt.ru и предоставляет данные через REST API.

### Архитектура сервера:

```
FastAPI приложение (main.py)
    ↓
Парсинг HTML с mpt.ru (parser.py)
    ↓
Кеширование в памяти (на 5 минут)
    ↓
REST API endpoints для iOS приложения
```

### Основные файлы сервера:

#### 1. **main.py** - FastAPI приложение
- Создаёт веб-сервер
- Настраивает CORS (разрешает запросы с iOS приложения)
- Реализует API endpoints (маршруты)
- Кеширует распарсенные данные

**Основные endpoints:**
- `GET /api/week-info` - информация о текущей неделе (Числитель/Знаменатель)
- `GET /api/specialties` - список всех специальностей
- `GET /api/groups?specialty_id=xxx` - группы для специальности
- `GET /api/schedule?group=xxx&specialty_id=xxx` - расписание группы
- `GET /api/replacements?group=xxx` - замены в расписании
- `GET /api/content/advertisements` - реклама/рекомендации
- `GET /api/content/news` - новости колледжа

#### 2. **parser.py** - Парсер HTML
- Загружает страницу `https://mpt.ru/raspisanie/` через httpx
- Парсит HTML с помощью BeautifulSoup
- Извлекает:
  - Тип недели (Числитель/Знаменатель)
  - Список специальностей
  - Группы для каждой специальности
  - Расписание пар для каждой группы
  - Замены в расписании

#### 3. **models.py** - Модели данных
- Определяет структуру данных для API (Pydantic модели)
- Валидирует данные перед отправкой клиенту

### Как работает парсинг:

1. **Загрузка страницы:**
   ```python
   async def fetch_page(url: str = BASE_URL) -> str:
       async with httpx.AsyncClient() as client:
           response = await client.get(url, timeout=30.0)
           return response.text
   ```

2. **Парсинг специальностей:**
   - Ищет `<ul class="nav-tabs">` - табы с названиями специальностей
   - Извлекает ID таба (tab_id) и название специальности

3. **Парсинг групп:**
   - Для каждой специальности находит `<div id="tab_id">`
   - Ищет внутри заголовки `<h3>Группа ...</h3>`

4. **Парсинг расписания:**
   - Находит таблицы с расписанием в нужной группе
   - Парсит строки таблицы: номер пары, предмет, преподаватель
   - Определяет сдвоенные пары (числитель/знаменатель)
   - Определяет территорию (Нахимовский/Нежинская)

5. **Кеширование:**
   - Распарсенный HTML сохраняется в памяти на 5 минут
   - При повторных запросах используется кеш (не парсим заново)

---

## 🔗 Связь приложения с сервером

### Протокол обмена данными:

1. **Приложение → Сервер**: HTTP GET запросы
2. **Сервер → Приложение**: JSON ответы

### Пример запроса и ответа:

**Запрос расписания:**
```
GET https://mpt-app.onrender.com/api/schedule?group=Э-1-22, Э-11/1-23&specialty_id=69d898df1add22061438dbc8ff0a73fa
```

**Ответ сервера (JSON):**
```json
{
  "week_info": {
    "date": "27 Ноября - Четверг",
    "week_type": "numerator",
    "week_type_ru": "Числитель"
  },
  "schedule": {
    "group": "Э-1-22, Э-11/1-23",
    "specialty_id": "69d898df1add22061438dbc8ff0a73fa",
    "days": [
      {
        "day": "ПОНЕДЕЛЬНИК",
        "day_index": 0,
        "campus": "Нахимовский",
        "lessons": [
          {
            "number": 3,
            "subject": "Электроника и схемотехника",
            "teacher": "Л.В. Дробышева",
            "subject_denominator": null,
            "teacher_denominator": null
          }
        ],
        "is_day_off": false
      }
    ]
  }
}
```

### Процесс загрузки данных в приложении:

1. **При открытии приложения:**
   ```swift
   // Сначала загружаем из кеша (мгновенно)
   if let cached = StorageService.shared.loadSchedule(for: group.id) {
       weekSchedules = cached
   }
   
   // Затем обновляем с сервера в фоне
   Task {
       await viewModel.loadSchedule(for: group)
   }
   ```

2. **Запрос к серверу:**
   ```swift
   // NetworkService.swift
   func fetchSchedule(group: String, specialtyId: String) async throws -> APIScheduleResponse {
       let url = URL(string: "\(baseURL)/api/schedule?group=\(group)&specialty_id=\(specialtyId)")!
       let (data, _) = try await URLSession.shared.data(from: url)
       return try JSONDecoder().decode(APIScheduleResponse.self, from: data)
   }
   ```

3. **Обработка ответа:**
   - Декодируем JSON в модели приложения
   - Сохраняем в кеш (UserDefaults)
   - Обновляем UI

### Обработка ошибок:

- Если нет интернета → показываем данные из кеша
- Если кеша нет → показываем mock-данные (тестовые)
- Все ошибки логируются, но не крашат приложение

---

## 🚀 Развёртывание на Render.com

### Почему Render.com?

- Бесплатный план для небольших проектов
- Автоматический деплой из GitHub
- HTTPS по умолчанию
- Сервер всегда работает (даже после бездействия)

### Как был настроен деплой:

1. **Создали аккаунт на Render.com**
   - Зарегистрировались через GitHub

2. **Создали новый Web Service:**
   - Выбрали репозиторий с кодом сервера
   - Тип: Web Service
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python main.py`

3. **Настроили переменные окружения:**
   - Render автоматически предоставляет переменную `PORT`
   - В коде: `port = int(os.environ.get("PORT", 8000))`

4. **Получили URL сервера:**
   - Render создал URL: `https://mpt-app.onrender.com`
   - Этот URL указан в `NetworkService.swift` в приложении

### Как работает сервер на Render:

1. При первом запросе сервер "просыпается" (может занять 30-60 секунд)
2. Сервер загружает и парсит страницу с mpt.ru
3. Кеширует данные на 5 минут
4. Отдаёт данные клиенту
5. После 15 минут бездействия сервер "засыпает" (free plan)

### Обновление сервера:

1. Изменяем код локально
2. Делаем `git push` в GitHub
3. Render автоматически перезапускает сервер с новым кодом

---

## 🔐 Безопасность и оптимизация

### Оптимизации:

1. **Кеширование на сервере** - данные парсятся раз в 5 минут, а не при каждом запросе
   - Экономит ресурсы сервера
   - Ускоряет ответы клиентам
   - Снижает нагрузку на сайт mpt.ru

2. **Кеширование в приложении** - данные сохраняются локально, приложение работает офлайн
   - Расписание кешируется в UserDefaults
   - Домашние задания хранятся локально
   - Контент (реклама, новости) кешируется
   - При отсутствии интернета показываются сохранённые данные

3. **Ленивая загрузка** - данные загружаются только когда нужны
   - Расписание загружается только для выбранной группы
   - Замены загружаются отдельно (они не критичны)
   - Рейтинги преподавателей загружаются при первом открытии экрана

4. **Фоновая загрузка** - обновления загружаются в фоне, не блокируя UI
   - Сначала показываются данные из кеша (мгновенно)
   - Затем в фоне обновляются с сервера
   - UI обновляется только после загрузки

5. **Проверка версий контента** - перед загрузкой всего контента проверяется версия
   - Быстрая проверка (занимает <1 секунды)
   - Полная загрузка только если версия изменилась
   - Автоматическая проверка каждые 5 минут

### Безопасность:

1. **CORS настройки** - сервер разрешает запросы только с нужных источников
   - В коде: `allow_origins=["*"]` (для тестирования)
   - В продакшене можно ограничить конкретными доменами

2. **Валидация данных** - все данные валидируются через Pydantic модели
   - Некорректные данные не попадут в ответ API
   - Защита от ошибок парсинга

3. **Обработка ошибок** - все ошибки обрабатываются корректно
   - Не крашат приложение
   - Показывают понятные сообщения пользователю
   - Логируются для отладки

4. **Локальное хранилище** - важные данные (ДЗ, настройки) хранятся локально
   - Не передаются на сервер
   - Приватность пользователя защищена

---

## 📱 Основные функции приложения

### 1. Просмотр расписания

**Экран "Сегодня":**
- Показывает расписание на текущий день
- Выделяет текущую пару
- Показывает замены (если есть)
- Можно добавить домашнее задание к каждой паре

**Экран "Неделя":**
- Расписание на всю неделю
- Разделение на числитель/знаменатель
- Цветовая индикация типа недели
- Переключение между неделями

### 2. Домашние задания

- Привязка ДЗ к конкретному уроку
- Учёт сдвоенных пар (отдельное ДЗ для числителя и знаменателя)
- Напоминания о дедлайнах
- Отметка о выполнении

### 3. Замены в расписании

- Автоматическая загрузка с сайта mpt.ru
- Фильтрация по выбранной группе
- Отображение на главном экране (если есть замены на сегодня)

### 4. Рейтинг преподавателей

- Голосование: 👍 Лайк, ➕/➖ Нейтрально, 👎 Дизлайк
- Режим голосования:
  - Открыто: Понедельник 00:00 - Суббота 17:00
  - Закрыто: Суббота 17:00 - Воскресенье 23:59
- Топ-3 лучших преподавателей
- Худшие преподаватели

### 5. Новости и рекомендации

- Новости колледжа с фотографиями
- Рекомендации: курсы, YouTube-каналы, Telegram-каналы
- Подборки ресурсов по категориям (программирование, безопасность, юриспруденция)
- Автоматическое обновление контента каждые 5 минут

### 6. Настройки

- Изменение выбранной группы
- Настройка цветов числителя/знаменателя
- Изменение размера текста
- Кастомные фото для числителя/знаменателя
- Очистка кеша

### 7. Расписание звонков

- Время начала и окончания каждой пары
- Продолжительность перемен

---

## 📊 Потоки данных

### Загрузка расписания:

```
Пользователь открывает приложение
    ↓
RootView проверяет выбор группы
    ↓
MainTabView открывается
    ↓
ScheduleViewModel загружает из кеша (мгновенно)
    ↓
Параллельно: запрос к серверу
    ↓
Сервер проверяет кеш (5 минут)
    ↓
Если кеш свежий → возвращает из памяти
Если кеш старый → парсит mpt.ru заново
    ↓
Ответ сервера (JSON)
    ↓
Приложение декодирует JSON
    ↓
Сохраняет в кеш (UserDefaults)
    ↓
Обновляет UI
```

### Обновление контента (реклама/новости):

```
Приложение запускается
    ↓
ContentUpdateService проверяет версию на сервере
    ↓
Если версия изменилась → загружает новый контент
Если версия та же → использует кеш
    ↓
Автоматическая проверка каждые 5 минут
```

---

## 🔧 Технические детали

### iOS приложение:

- **Язык**: Swift 5.9+
- **Фреймворк**: SwiftUI
- **Минимальная версия iOS**: iOS 16.0+
- **Архитектура**: MVVM (Model-View-ViewModel)
- **Хранение данных**: UserDefaults
- **Сетевые запросы**: URLSession с async/await

### Сервер:

- **Язык**: Python 3.10+
- **Фреймворк**: FastAPI
- **Парсинг**: BeautifulSoup4 + lxml
- **HTTP клиент**: httpx (асинхронный)
- **Хостинг**: Render.com (Free plan)
- **Тип сервера**: Web Service

### API Endpoints:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/week-info` | Информация о неделе (Числитель/Знаменатель) |
| GET | `/api/specialties` | Список специальностей |
| GET | `/api/groups?specialty_id=xxx` | Группы для специальности |
| GET | `/api/schedule?group=xxx&specialty_id=xxx` | Расписание группы |
| GET | `/api/replacements?group=xxx` | Замены в расписании |
| GET | `/api/teachers` | Список всех преподавателей |
| GET | `/api/content/advertisements` | Реклама/рекомендации |
| GET | `/api/content/news` | Новости |
| GET | `/api/content/version` | Версия контента |

---

## 📝 Итоговая схема работы

```
┌─────────────────┐
│  iOS Приложение │
│   (SwiftUI)     │
└────────┬────────┘
         │
         │ HTTP GET запросы
         │ (JSON)
         ↓
┌─────────────────┐
│   Render.com    │
│  (Python/FastAPI)│
└────────┬────────┘
         │
         │ Парсинг HTML
         │ (BeautifulSoup)
         ↓
┌─────────────────┐
│    mpt.ru       │
│  (Расписание)   │
└─────────────────┘

Кеширование:
- Сервер: 5 минут (в памяти)
- Приложение: локально (UserDefaults)
```

---

## 💡 Важные моменты для защиты

### Что подчеркнуть при рассказе:

1. **Архитектура**:
   - Чёткое разделение на слои (UI, ViewModel, Network, Storage)
   - Использование современных паттернов (MVVM, async/await)
   - Кеширование на нескольких уровнях

2. **Оптимизация**:
   - Работа офлайн благодаря кешированию
   - Быстрая загрузка (сначала кеш, потом обновления)
   - Эффективное использование ресурсов сервера

3. **Надёжность**:
   - Обработка всех ошибок
   - Graceful degradation (при ошибке показываем кеш)
   - Автоматическое обновление данных

4. **Пользовательский опыт**:
   - Мгновенный отклик (кеш)
   - Красивый интерфейс (SwiftUI)
   - Персонализация (выбор группы, настройки)

5. **Масштабируемость**:
   - Легко добавить новые endpoints
   - Кеширование снижает нагрузку
   - Модульная архитектура

---

## 🎓 Заключение

Проект MPT App - это полнофункциональное мобильное приложение с собственным backend-сервером. Приложение работает офлайн, автоматически обновляет данные, имеет красивый интерфейс и множество полезных функций для студентов.

Сервер развёрнут на бесплатном хостинге Render.com и работает стабильно. Приложение готово к использованию студентами МПТ.

