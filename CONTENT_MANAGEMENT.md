# 📱 Управление контентом в приложении MPT

## 🎯 Обзор системы обновлений

Приложение MPT использует **ContentUpdateService** для управления рекламой и новостями. Контент можно обновлять удаленно без выпуска новой версии приложения.

## 📊 Текущая реклама в приложении

Сейчас в приложении показываются реальные курсы IT-образования:

1. **Skillbox** — IT курсы со скидкой для студентов 20%
2. **GeekBrains** — Программирование с трудоустройством  
3. **Яндекс.Практикум** — Data Science, дизайн, маркетинг
4. **Нетология** — Digital образование с дипломом гос. образца
5. **HTML Academy** — Веб-разработка
6. **Stepik** — Бесплатное образование
7. **Coursera** — Университетские курсы
8. **Udemy** — Практические навыки

## 🔧 Как добавить новую рекламу

### Способ 1: Локальное обновление (требует обновления приложения)

Отредактируйте файл `ContentUpdateService.swift`:

```swift
private func fetchAdvertisements() async throws -> [Advertisement] {
    return [
        // Добавьте новую рекламу здесь
        Advertisement(
            title: "Название курса/сервиса",
            description: "Описание предложения до 80 символов",
            imageName: "logo_name", // Опционально
            url: "https://example.com",
            category: .course // или .onlineSchool, .service
        ),
        // ... остальные
    ]
}
```

### Способ 2: Удаленное обновление (рекомендуется)

#### Настройка API endpoint на сервере:

1. Добавьте в `server/main.py`:

```python
@app.get("/api/content/advertisements")
async def get_advertisements():
    advertisements = [
        {
            "title": "Skillbox — IT курсы",
            "description": "Получи востребованную профессию в IT. Скидка для студентов 20%",
            "imageName": "skillbox_logo",
            "url": "https://skillbox.ru",
            "category": "onlineSchool"
        }
        # ... добавьте больше рекламы
    ]
    return {"advertisements": advertisements}
```

2. Обновите `ContentUpdateService.swift`:

```swift
private func fetchAdvertisements() async throws -> [Advertisement] {
    let url = URL(string: "\(NetworkService.shared.baseURL)/api/content/advertisements")!
    let (data, _) = try await URLSession.shared.data(from: url)
    let response = try JSONDecoder().decode([String: [Advertisement]].self, from: data)
    return response["advertisements"] ?? []
}
```

## 📰 Управление новостями

### Добавление фотографий:

1. Добавьте изображение в папку `MPTapp/Assets.xcassets/news/`
2. Обновите список новостей в `ContentUpdateService.swift`:

```swift
private func fetchNews() async throws -> [NewsItem] {
    return [
        NewsItem(imageName: "новое_фото", title: "Заголовок", description: "Описание"),
        // ... остальные новости
    ]
}
```

### Удаленное обновление новостей:

Создайте endpoint `/api/content/news` аналогично рекламе.

## ⚡ Автоматические обновления

### Как это работает:

1. **При запуске** — контент загружается из кеша (мгновенно)
2. **В фоне** — проверяется сервер на наличие обновлений
3. **Каждый час** — автоматическая проверка обновлений
4. **Fallback** — если сервер недоступен, используется кеш

### Принудительное обновление:

```swift
ContentUpdateService.shared.forceUpdate()
```

## 📈 Мониторинг рекламы

### Добавление аналитики кликов:

1. Обновите `AdvertisementCard` в `NewsView.swift`:

```swift
Button(action: {
    // Аналитика клика
    trackAdvertisementClick(advertisement.title)
    // Открываем ссылку
    if let url = URL(string: advertisement.url ?? "") {
        UIApplication.shared.open(url)
    }
}) {
    // UI карточки
}
```

2. Создайте функцию аналитики:

```swift
private func trackAdvertisementClick(_ adTitle: String) {
    // Отправляем статистику на сервер
    Task {
        try? await NetworkService.shared.trackClick(adTitle)
    }
}
```

## 🎨 Кастомизация рекламы

### Размеры и стили:

- **Высота карточки**: 120px
- **Автопрокрутка**: каждые 4 секунды
- **Анимация**: плавная пружинная (0.8 секунды)

### Категории рекламы:

```swift
enum AdCategory: String, Codable {
    case course = "course"           // Курсы
    case onlineSchool = "onlineSchool" // Онлайн-школы
    case service = "service"         // Сервисы
}
```

## 🔄 План внедрения удаленных обновлений

### Этап 1: Подготовка сервера
- [ ] Создать endpoints для контента
- [ ] Добавить админ-панель для управления

### Этап 2: Обновление приложения  
- [ ] Подключить API к ContentUpdateService
- [ ] Добавить обработку ошибок
- [ ] Протестировать на разных сценариях

### Этап 3: Мониторинг
- [ ] Добавить аналитику просмотров
- [ ] Отслеживать клики по рекламе
- [ ] A/B тестирование разных предложений

## 💡 Рекомендации по контенту

### Для рекламы:
- **Заголовок**: до 30 символов
- **Описание**: до 80 символов
- **Цель**: образование/развитие студентов
- **CTA**: четкий призыв к действию

### Для новостей:
- **Формат изображений**: 16:9, минимум 800x450px
- **Содержание**: события колледжа, достижения студентов
- **Периодичность**: 2-3 обновления в неделю

---

**💬 Нужна помощь?**
Обратитесь к разработчику для настройки удаленных обновлений или добавления новых функций.
